{
  "name": "phx-ctf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && next build",
    "export": "next export",
    "start": "next start",
    "lint": "next lint",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "setup:production": "node setup-production.js",
    "db:seed": "curl -X POST http://localhost:3000/api/seed",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "postbuild": "prisma generate",
    "deploy:netlify": "npm run build && netlify deploy --prod",
    "deploy:preview": "npm run build && netlify deploy",
    "deploy:prod": "netlify deploy --prod",
    "deploy:prod-open": "netlify deploy --prod --open",
    "deploy:no-build": "netlify deploy --no-build",
    "deploy:create": "netlify deploy --create-site",
    "deploy:trigger": "netlify deploy --trigger",
    "deploy:message": "netlify deploy --message",
    "deploy:site": "netlify deploy --site",
    "build:netlify": "npm run build",
    "build:cpanel": "npm run build && npm run export",
    "build:vercel": "npm run build",
    "deploy:vercel": "vercel --prod",
    "deploy:vercel-preview": "vercel",
    "type-check": "tsc --noEmit",
    "build:analyze": "ANALYZE=true npm run build",
    "netlify:login": "netlify login",
    "netlify:status": "netlify status",
    "netlify:logs": "netlify logs",
    "netlify:env:set": "netlify env:set",
    "netlify:env:list": "netlify env:list"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0",
    "@next-auth/prisma-adapter": "^1.0.7",
    "@prisma/client": "^6.15.0",
    "@supabase/supabase-js": "^2.57.4",
    "@types/bcryptjs": "^2.4.6",
    "@types/uuid": "^10.0.0",
    "bcryptjs": "^3.0.2",
    "next": "14.0.4",
    "next-auth": "^4.24.5",
    "react": "^18",
    "react-dom": "^18",
    "react-hook-form": "^7.48.2",
    "react-hot-toast": "^2.4.1",
    "uuid": "^11.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@netlify/plugin-nextjs": "^5.7.2",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "eslint": "^8",
    "eslint-config-next": "14.0.4",
    "postcss": "^8",
    "prisma": "^6.15.0",
    "tailwindcss": "^3.3.0",
    "typescript": "^5"
  }
}
